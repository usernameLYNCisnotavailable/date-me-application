rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Store avatars at avatars/{uid}/filename
    match /avatars/{uid}/{allPaths=**} {
      allow read:  if true;   // avatars are public
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
